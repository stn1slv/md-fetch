# Quickstart: mdfetch-cli

Once installed or running via `uv run`, you can use the `md-fetch` command directly from your terminal.

## Print to stdout
```bash
md-fetch https://medium.com/example/article
```

## Save to a file
```bash
md-fetch https://dev.to/example/article --output my_article.md
```
